package org.example.kursovabd.repositories;

import org.example.kursovabd.data.Restorer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RestorerRepository extends JpaRepository<Restorer, Integer> {
}